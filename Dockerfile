# Generated by https://smithery.ai. See: https://smithery.ai/docs/build/project-config
FROM python:3.11-slim

# Install Chromium for Selenium
RUN apt-get update \
    && apt-get install -y chromium chromium-driver \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Patch server to use dicts for capabilities
RUN sed -i "s/notification_options=NotificationOptions([^)]*)/notification_options=NotificationOptions(tools_changed=False, resources_changed=False, prompts_changed=False).__dict__/" src/server.py \
    && sed -i "s/experimental_capabilities=ExperimentalCapabilities()/experimental_capabilities=ExperimentalCapabilities().__dict__/" src/server.py

ENV PYTHONPATH=.
CMD ["python", "-m", "src.server"]
