# Generated by https://smithery.ai. See: https://smithery.ai/docs/build/project-config
FROM python:3.10-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .

# Patch server to pass dicts for capabilities
RUN sed -i 's/notification_options=NotificationOptions(\([^)]*\))/notification_options=NotificationOptions(\1).__dict__/g' src/server.py \
    && sed -i 's/experimental_capabilities=ExperimentalCapabilities()/experimental_capabilities=ExperimentalCapabilities().__dict__/g' src/server.py

ENV PYTHONPATH=/app
ENV PYTHONUNBUFFERED=1
CMD ["python", "-m", "src.server"]
